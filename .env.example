# OpenAI-compatible LLM endpoint (swap to Kompact CPU runtime in Phase 2)
BASE_URL=https://api.openai.com/v1
API_KEY=
MODEL=gpt-4o-mini

# Optional: leave API_KEY empty to run deterministic MOCK packs (no network)
